# Nextcloud_user_batch_add
Python script to inject many users including group settings into nextcloud and generate a random password for the new users including sending the credentials to the new users. Also set the change password at next login toggle
